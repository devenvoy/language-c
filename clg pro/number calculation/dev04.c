/* Take a number(n) from user & output its cube(n*n*n).*/

#include<stdio.h>
main(){
	float a;
	printf("n\Enter side of the cube:");
	scanf("%f" ,&a);
	printf("Output its cube:%f", a*a*a);
	return 0;
}
